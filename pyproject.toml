[tool.poetry]
name = "fyreplace-api"
version = "0.0.0"
description = "Fyreplace API"
authors = []
license = "AGPL-3.0-or-later"
readme = "README.md"
packages = []

[tool.poetry.dependencies]
black = "^23.1"
celery = {extras = ["redis"], version = "^5.2"}
django = {extras = ["argon2"], version = "^4.1"}
django-anymail = {extras = ["mailgun"], version = "^9.0"}
django-celery-beat = "^2.4"
django-extensions = "^3.2"
django-storages = {extras = ["boto3"], version = "^1.13"}
django-stubs = "^1.15"
email-validator = "^1.3"
firebase-admin = "^6.1"
grpcio = "^1.51"
grpcio-tools = "^1.51"
grpc-interceptor = "^0.15"
httpx = {extras = ["http2"], version = "^0.23"}
ipython = "^8.10"
isort = "^5.12"
mysqlclient = "^2.1"
pillow = "^9.4"
psycopg2 = "^2.9"
pyjwt = {extras = ["crypto"], version = "^2.6"}
pytest-xdist = {extras = ["psutil"], version = "^3.2"}
pytest-django = "^4.5"
python = "^3.11"
python-dotenv = "^0.21"
python-magic = "^0.4"
qrcode = "^7.4"
requests = "^2.28"
rollbar = "^0.16"
tqdm = "^4.64"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
